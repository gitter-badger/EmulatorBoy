#ifndef EMULATORBOY_INCLUDE_REGISTERS_HPP
#define EMULATORBOY_INCLUDE_REGISTERS_HPP

/**
 * Each Processor contains a set of registers that should be
 * represented as one container object that extends this class.
 */
class Registers {

};

#endif